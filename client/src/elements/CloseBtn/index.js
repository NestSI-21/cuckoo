import React from 'react';
import { close } from './closebtn.module.scss';
const CloseBtn = () => {
  return (
    <div>
      <button id={close}>+</button>
    </div>
  );
};

export default CloseBtn;
