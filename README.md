# cuckoo

# backend and frontend

======================

- Setup
  `docker-compose build`

- Run app
  `docker-compose up`

- Update Gemfile with new gems and install those gems
  `docker-compose build`

# frontend only

===============

- Setup
  `yarn`

- Run app
  `yarn start`
